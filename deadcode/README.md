# Archived / unused code

Moved out of the active `src/` tree so the app build stays lean. Not imported by the live app.

| Path | Reason |
|------|--------|
| `Character/` | 3D avatar removed from portfolio |
| `WhatIDo.tsx`, `WhatIDo.css` | Skills accordion replaced by tech stack section |
| `GsapScroll.ts` | Character scroll timelines; replaced by `scrollTimelines.ts` |
| `skillHighlight.ts` | Only used by WhatIDo |
| `boneData.ts` | Only used by Character animations |

Copy of `Character/` also lives here; original may remain under `src/components/Character` if Windows locked the folder during move.
